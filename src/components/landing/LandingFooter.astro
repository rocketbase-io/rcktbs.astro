---
/**
 * LandingFooter Component (i18n-aware)
 * Footer for landing pages with translations
 */
import rocketbaseLogo from '@/assets/rocketbase/logo.svg';
import Icon from '@/components/ui/primitives/Icon/Icon.astro';
import { type Locale, getLocaleFromPath } from '@/i18n/config';
import { useTranslations, getLocalizedPath } from '@/i18n/index';

interface Props {
  locale?: Locale;
}

const { locale: localeProp } = Astro.props;

// Get locale from prop or infer from URL
const locale = localeProp || getLocaleFromPath(Astro.url.pathname);
const t = useTranslations(locale);

const links = [
  { label: t('nav.services'), href: getLocalizedPath('services', locale) },
  { label: t('nav.about'), href: getLocalizedPath('about', locale) },
  { label: t('nav.contact'), href: getLocalizedPath('contact', locale) },
  { label: 'GitHub', href: 'https://github.com/rocketbase-io' },
  { label: 'LinkedIn', href: 'https://www.linkedin.com/company/rocketbase-io/' },
];
---

<footer class="border-t border-border bg-background-secondary py-12">
  <div class="mx-auto flex max-w-6xl flex-col gap-6 px-6 md:flex-row md:items-center md:justify-between">
    <div class="flex flex-col gap-3">
      <a href={getLocalizedPath('home', locale)} class="flex items-center">
        <img src={rocketbaseLogo.src} alt="Rocketbase" class="h-8 w-auto dark:invert" />
      </a>
      <p class="max-w-md text-sm text-foreground-muted">{t('site.description')}</p>
    </div>

    <nav class="flex flex-wrap gap-6 text-sm font-medium text-foreground-muted">
      {links.map((link) => (
        <a
          href={link.href}
          class="hover:text-foreground transition-colors"
          target={link.href.startsWith('http') ? '_blank' : undefined}
          rel={link.href.startsWith('http') ? 'noopener noreferrer' : undefined}
        >
          {link.label}
        </a>
      ))}
    </nav>
  </div>
</footer>
